# Moodle Memcached+ Cache Store

Key-aware Memcached store for Moodle.

Additional features to core Memcached:
 * Session type support
 * Definition-aware purge()
 * Self-locking
 * Searchable
 * Key-aware
